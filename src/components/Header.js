import React from 'react';

const Header = () => {
    return (
        <header>
            <h1>My App</h1>
            {/* Other header content */}
        </header>
    );
};

export default Header;
